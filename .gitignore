# Python
__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/

# Vector store — se genera localmente, no se commitea
mcp_server/chroma_db/

# Datos reales — no subir al repo público
# Descomentar las líneas que correspondan según el nivel de privacidad deseado

# datos/nps/
# datos/soporte/
# datos/entrevistas/

# Si los datos no tienen información personal identificable (PII),
# se pueden incluir en el repo. Si tienen nombres, emails o datos de clientes,
# excluirlos y compartirlos solo por canales internos seguros.
